# toss 격차 기록

`node scripts/gap.mjs [경로] [단계수]` 로 toss.im 과 배포본을 같은 스크롤 깊이에서
나란히 찍는다. 왼쪽 열이 toss, 오른쪽 열이 우리, 행이 스크롤 깊이다.

격차를 매번 사람이 찾아내야 했기 때문에 만들었다. 비교가 싸지면 변경마다 돌릴 수 있다.

## 회차 1 — 히어로가 정지 루프였다

| | |
|---|---|
| 고른 격차 | 스크롤해도 장면이 전개되지 않고 5초 루프가 제자리에서 돌았다. 문구도 고정이었다. |
| 근거 | toss 는 프레임 000→001 에서 헤드라인이 밀려나며 흐려지는 동시에 다음 장면이 자리를 넘겨받고 새 문구가 얹힌다 |
| 고친 것 | 섹션을 네 화면 높이로 두고 안쪽 무대를 sticky 로 붙였다. 스크롤이 무대를 밀어내는 대신 장면을 진행시킨다. 장면과 문구가 blur+opacity 로 함께 교체된다. |
| 부수적으로 | 영상 소재도 바꿨다. 아트리움은 AI대학과 무관한 빈 건물이었다. 네 장면이 ADP+X 가치사슬 자체를 설명하게 했다. |
| 재측정 | 좁혀짐. 행 0~2 에서 두 사이트가 같은 종류의 화면으로 읽힌다. |

## 회차 2 — 히어로가 끝나면 문서로 무너졌다

| | |
|---|---|
| 고른 격차 | 행 3~4 에서 toss 는 끝까지 화면을 채우는 장면이 이어지는데 우리는 정의 목록 나열이 된다 |
| 근거 | `assets/gap/home.jpg` 행 3 — toss 는 다른 요소가 거의 없는 화면에 "온오프라인 경계 없이" 한 문장만 크게 놓는다. 정보 밀도가 높은 구간 사이에 숨 쉬는 화면을 끼워 리듬을 만든다. |
| 고친 것 | 팩트시트 앞에 424 를 놓은 전체 화면 선언을 두었다. 표 안의 한 행으로 묻혀 있던 가장 강한 증거를 화면 하나로 올렸다. 이어지는 표도 2열 격자로 바꿔 각 항목이 읽을 거리를 갖게 했다. |
| 바꾼 것 | toss 는 검정 배경을 쓰지만 브랜드 블루 면을 썼다. 국립대 페이지에 검정 인터스티셜은 톤이 맞지 않는다. |
| 재측정 | 좁혀짐. 행 3 에서 양쪽 모두 전체 화면 선언이 됐다. |

## 남은 격차

- **행 4** — toss 는 어두운 풀블리드 제품 장면이 계속되는데 우리는 표가 이어진다.
  스펙 표를 없앨 수는 없다. 표 자체를 장면처럼 다루는 방법을 찾아야 한다.
- **미디어 종류가 한 가지다.** toss 는 실사 영상·제품 목업·카드 레일·다크 카드를
  계속 교차시킨다. 우리는 유리 오브젝트 하나로 전 페이지를 끌고 간다.
  실사 인물 컷은 검증에서 전량 불합격 나 폐기했다(깨진 텍스트·비한국인·금지색).
  다시 시도하려면 그 결함을 프롬프트에 명시해야 한다.
- **배경 전환이 적다.** toss 는 흰색 → 검정 → 흰색 → 남색으로 장마다 바뀐다.
  우리는 424 블루 면이 유일한 강조면이다.

## 회차 3 — 대기열이 순서에 막혀 완성품을 못 가져왔다

| | |
|---|---|
| 고른 격차 | 전환 모프가 완성돼 있는데 화면에 없었다 |
| 원인 | 대기열이 목록 순서대로 처리해서, 슬롯을 기다리는 항목 하나가 뒤에 있는 완료분 전부를 막고 있었다 |
| 고친 것 | 2패스로 바꿨다. 첫 패스가 이미 job id 가 있는 것을 전부 회수하고, 두 번째 패스가 미제출분을 제출한다 |
| 재측정 | `t_da` `t_ap` 회수·변환 완료 |

## 회차 4 — 팩트 구간에 미디어가 없었다

| | |
|---|---|
| 고른 격차 | 행 4 에서 toss 는 풀블리드 제품 장면인데 우리는 미디어 없는 표만 이어진다 |
| 근거 | `assets/gap/home.jpg` 행 4 |
| 고친 것 | toss §2-2 의 무대 고정 구조를 팩트 구간에 적용했다. 왼쪽 오브젝트를 sticky 로 붙이고 오른쪽 팩트 목록이 지나간다. 읽고 있는 항목만 선명하고 나머지는 물러난다. 항목이 속한 묶음이 바뀌면 오브젝트도 교체된다. |
| 판단 | 스크롤 비율이 아니라 뷰포트 중앙에 가장 가까운 행을 기준으로 삼았다. 행 높이가 고르지 않아 비율로 잡으면 아직 이전 묶음을 읽는 중에 오브젝트가 바뀐다. |
| 재측정 | 좁혀짐. 프레임 007→008 에서 오브젝트가 교체되고 현재 항목만 선명하다. |
| 같은 회차 | 히어로 담당(codex)이 모프 배선을 마쳤다. 프레임 001·004 에 변형 중간 상태가 잡힌다. |

## 5회차 — 히어로 영상을 클라이언트 편집본으로 교체

**클라이언트 지적**: "내가 만든 영상이 훨 나은데 코덱스가 만든 것보다. 왤케 추상적으로 만들어."

맞다. 원인은 코덱스가 아니라 내가 잡은 형태 언어다. 라이브액션 자산이 품질
검수에서 떨어진 뒤 "밝은 갤러리에 놓인 반투명 유리 오브젝트"로 고정했고,
재생성할 때마다 그 언어를 계속 강제했다. 그 결과 어느 컷도 한 마디로
무엇인지 말할 수 없었다. 연구실도 데이터도 공정도 부산대도 보이지 않았다.

`spliced.mp4` 는 컷마다 주어가 있다. 로봇 팔이 있는 연구실 / 데이터 비 /
궤도를 도는 입자 구름 / 자율주행차 / 기어를 조립하는 산업 로봇 / 부산대
엠블럼. 사이는 워프로 넘어간다.

| 슬롯 | 구간 | 무엇이 보이나 |
|---|---|---|
| 히어로 전체 | 0–10s | 스크롤에 물린 편집본 전체 |
| axis_d | 2.05–3.32 | 데이터 비 |
| axis_a | 3.45–4.36 | 입자 구름 + 궤도 링 |
| axis_x | 5.75–6.62 | 자율주행차 |
| axis_p | 6.80–7.92 | 산업 로봇 + 기어 |
| axis_stat | 2.60–3.30 | 데이터 비 (왼쪽 크롭, 0.5배속) |
| schools | 4.60–5.50 | 워프 (출범) |
| apex | 1.25–1.92 | 연구실 전경 (0.5배속) |
| infra_gpu | 0.25–1.15 | 로봇 손 + 홀로 인터페이스 |
| chain | 4.95 | 워프 정지컷 |
| roadmap_path | 6.05–6.62 | 차선이 뻗는 화면 (0.5배속) |
| emblem | 9.5 | 부산대 엠블럼 정지컷 |

남긴 추상: `asis` / `tobe` 뿐이다. "지금 흩어져 있고 앞으로 맞물린다"는
비교 자체가 은유라 흩어진 파편과 완성된 다면체가 영상 어느 컷보다 정확하다.

**측정**: 배포본 재캡처에서 행 0 헤드라인이 60% 불투명도·4px 블러로
착지하던 것을 발견해 인트로 문구 창을 0 에서 시작하도록 고쳤다. 재측정에서
또렷하게 착지한다.

**크레딧**: 25.68 남음. 힉스필드 재생성은 사실상 끝났고, 앞으로 자산이
필요하면 이 편집본에서 잘라 쓴다.

## 6회차 — 워프가 스크롤을 먹던 것

**측정**: 배포본을 toss 와 6단계로 나란히 찍었다. 행 2 와 행 3 이 **둘 다 워프**였다.
문구 없는 화면이 두 스크린 연속이다. toss 는 여섯 행 전부 문구가 있다.

**원인**: 스크롤 진행률을 영상 시간에 그대로 비례시켰다. 4.40–5.67 워프는
1.27초뿐인데 전체의 12.7%, 700svh 중 89svh 를 가져갔다. 영상 안에서는
1.27초에 지나가는 전환이 스크롤에서는 한 스크린 가까이 늘어난 셈이다.

**고침**: 컷별 스크롤 배분(`SEGMENTS`)을 도입해 워프 두 구간만 0.28배로 눌렀다.

| 컷 | 전 | 후 |
|---|---|---|
| 워프 (4.40–5.67) | 89svh | 28svh |
| 워프 (7.96–8.40) | 31svh | 10svh |

나머지 컷은 80–152svh 를 받는다. 편집은 그대로다. 바뀐 것은 스크롤 한 칸이
몇 초를 넘기느냐뿐이다.

**재측정**: 로컬 14단계에서 히어로 표본 10개가 전부 문구를 달고 있고 문구
없는 화면은 하나도 잡히지 않았다. 배포 후 6단계 재측정에서도 행 0–3 이
각각 헤드라인 / D / X / 엠블럼으로 채워졌다. 좁혀졌다.

**힉스필드 대기열**: 정리했다.
- `hf-queue.mjs` 가 `execFile` 에 인자까지 붙은 한 줄을 넘기고 있었다.
  PowerShell 에서는 우연히 통과하지만 bash 에서는 ENOENT 로 죽는다. `exec` 로 고쳤다.
- 죽은 작업 하나가 큐 전체를 멈추던 것도 고쳤다. `Job not found` 는 이제
  그 항목만 버리고 넘어간다.
- `scene_d/a/p/x` 네 건은 작업이 만료돼 복구 불가. 재제출하면 40크레딧인데
  잔액이 25.68 이고, 히어로가 클라이언트 편집본으로 바뀌어 쓰이지도 않는다. 제거.
- `t_px` 는 seedance 45크레딧이라 제출 자체가 불가능. 제거.
- **힉스필드 대기열은 이것으로 끝이다.** 앞으로 자산은 편집본에서 잘라 쓴다.

**다음 격차**: toss 행 3 은 화면 밖으로 잘려나가는 가로 카드 레일이다.
우리 행 4 는 3열 고정 그리드라 더 볼 것이 있다는 신호가 없다.

## 7회차 — 가로로 움직이는 것이 하나도 없었다

**관찰**: toss 를 0.470~0.545 여섯 단계로 찍어 비교했다.
`assets/motion/toss-rail-move.jpg`

- 0.485 — 레일 맨 왼쪽이 「혜택이 잘 보이는 리스트 광고」
- 0.500 — 맨 왼쪽이 「자동 플레이되는 동영상 광고」, 앞 두 장은 왼쪽으로 잘려나감

페이지가 1.5% 내려가는 동안 레일이 **카드 두 장만큼 왼쪽으로 갔다.** 제목은
제자리고 레일만 움직인다. CSS 를 읽어서가 아니라 두 프레임을 나란히 놓고 확인했다.

**격차**: 우리에게는 가로로 움직이는 것이 하나도 없었다. 바로 가기는 3열 고정
그리드라 카드가 양끝에서 딱 끊긴다 — 셋이 전부라는 인상이다. 홈에는 학부 섹션
자체가 없기도 했다. CORE.md 가 요구한 편제 대상 학부·학과가 서브페이지에만 있었다.

**고침**: `SchoolRailScene` — 학부 다섯 곳을 세로 스크롤이 미는 가로 레일로.

- 이동 거리를 상수로 박지 않고 매번 `scrollWidth - clientWidth` 를 잰다.
  카드 폭과 화면 폭이 뷰포트마다 달라 상수로 두면 좁은 화면에서 끝까지 못 가고
  넓은 화면에서는 빈 자리가 생긴다.
- 레일을 `edge` 컨테이너 **밖**에 뒀다. 안에 가두면 양끝에서 딱 끊겨 더 있다는
  신호가 사라진다.
- 카드 영상은 5회차에 편집본에서 잘라둔 축별 화면을 그대로 쓴다.

**잡은 것**: D축 두 단위는 원문에 학부 단독 정원이 없어 합산 114명만 있다.
`D축 114명 (통계학과 포함)` 으로 적었더니 **통계학과 카드에 그대로 찍혀 자기를
가리켰다.** `D축 합산 114명` 으로 바꿨다.

**재측정**: 배포 후 7단계.

| 행 | toss | 우리 |
|---|---|---|
| 0 | 전면 영상 + 헤드라인 | 전면 영상 + 헤드라인 |
| 2 | 전면 제품 화면 | 전면 영상 |
| 6 | 「토스가 바꾼 일상」 + 사진 카드 줄 | 「424명이 이 다섯 곳으로」 + 영상 카드 줄 |

행 6 이 같은 형태가 됐다. 좁혀졌다.

**다음 격차**: 히어로가 끝나자마자 바로 가기 3열 카드가 붙고 그 뒤에 424 블루 판이
바로 온다. 밀도 높은 블록이 쉬는 화면 없이 이어진다. toss 는 밀도 높은 구간 사이에
문장 하나만 놓인 화면을 넣는다 (§2-8 「온오프라인 경계 없이」).

## 8회차 — 바로 가기 제거, 한눈에 보기 재작업

**클라이언트 지적**: 바로 가기 3열 카드 없애고, 한눈에 보기부터 처참하다.

원본 해상도(1440px)로 다시 보니 세 가지가 겹쳐 있었다.

1. **평평한 파란 사각형 + 엠블럼 워터마크.** 히어로는 여백을 둔 둥근 카드인데
   이건 각진 전면 사각형이라 기하가 충돌한다. 파란 면 자체도 빈 판이었다.
2. **읽고 있지 않은 행을 0.4 로 죽였다.** 여덟 줄 중 일곱 줄이 회색이라 강조가
   아니라 로드 실패로 보였다. toss 는 본문을 죽이는 일이 없다. 5회차에 toss
   §2-2 무대 고정을 옮겨온 것인데, toss 는 **무대를 고정하고 카피를 교체**하지
   본문 목록을 흐리지 않는다. 패턴을 잘못 옮겼다.
3. **왼쪽 정사각 영상 + 오른쪽 긴 표.** 표가 훨씬 길어 영상 아래로 빈 공간이
   화면 절반 넘게 남았다. 제목 「부산대학교 AI대학」은 고정 내비에 걸려 잘렸다.

**다시 짠 구성** — toss 리듬 그대로.

| 자리 | 내용 | toss 근거 |
|---|---|---|
| 쉬는 화면 | 흰 바탕 424 하나 + 214/114/69/27 한 줄 | §2-8 흰 화면 문장 하나 / Toss Ads 큰 문장 밑 지표 줄 |
| 이미지 띠 | 글자 없는 연구실 화면, 히어로와 같은 둥근 카드 | 제품 사진 구간 (프레임 015~018) |
| 표 | 규모 / 구조 / 준비 상황 세 묶음, 전부 또렷하게 | — |

띠는 처음에 apex(560x560)를 3:1 로 늘려 흐렸다. 원본에서 1920x640 을 잘라
1600 폭 `band_lab` 으로 새로 뽑았다.

**곁가지로 잡은 것**: 바로 가기를 걷어내니 히어로 CTA 두 개가 다 어긋나 있었다.
첫 버튼은 「한눈에 보기」인데 `/ai-college` 로 갔고, 둘째는 `#adpx` 를 가리키는데
홈에 그런 요소가 없다 (ADP+X 절은 `/ai-college` 에 있다). 라벨을 목적지에 맞췄다.
히어로가 이제 AI대학 페이지로 가는 유일한 입구다.

**옮긴 것**: 바로 가기에만 있던 A.U.R.A / Google for Education 은 CORE.md
요구사항이라 푸터 「함께 보기」로 옮겼다. `GatewayScene.jsx` 는 지우지 않고 뒀다.

**재측정**: 배포 후 6단계에서 행 4 의 파란 사각형이 사라지고 넓은 이미지 띠 +
「규모」 묶음이 자리했다. 좁혀졌다.

**남은 격차**: 표의 라벨 열과 값 열 사이가 넓은 화면에서 지나치게 벌어진다.
toss 행 4 는 같은 자리에 제품 사진이 있다.

## 9회차 — 히어로 일곱 화면이 전부 같은 구도

**측정**: 배포본 6단계. 행 1·2·3 이 서로 구분되지 않았다. 전면 어두운 영상 +
왼쪽 아래 흰 문구. 세 번 같은 화면이다.

toss 는 행마다 구도가 바뀐다 — 사진 위 왼쪽 아래 / 흰 바탕 카피 왼쪽·제품 오른쪽 /
제품 왼쪽·카피 오른쪽 / 헤드라인 밑 카드 줄.

**원인**: 히어로가 700svh 로 페이지의 60% 인데 여덟 챕터가 한 자리를 공유했다.

**고침**: 컷마다 문구 자리를 다르게. 임의로 고르지 않고 그 컷에 무엇이 어디
있는지를 따랐다.

| 컷 | 피사체 | 문구 자리 |
|---|---|---|
| 인트로 | 로봇 팔이 위쪽 절반 | 왼쪽 아래 |
| D | 데이터 비, 고른 질감 | 오른쪽 아래 (앞뒤와 엇갈리게) |
| A | 입자 구름, 한가운데·성김 | 가운데 |
| X | 자율주행차가 오른쪽 | 왼쪽 아래 |
| P | 로봇 팔이 왼쪽 | 오른쪽 아래 |
| 마무리 | 엠블럼이 한가운데 | 왼쪽 아래 |

마무리도 처음엔 가운데였는데 엠블럼은 형태가 꽉 차서 글자와 겹치면 둘 다 안
읽혔다. 입자 구름은 성기어서 가운데가 그대로 된다. 프레임으로 확인하고 바꿨다.

가림막을 두 겹으로 나눴다. 아래에 앉는 컷은 아래쪽만, 가운데 앉는 컷은 화면
전체를 45% 로. 두 겹의 불투명도를 각 컷 가중치 합으로 굴려 자리를 옮길 때
가림막도 같이 넘어간다. 따로 끊으면 전환 순간에 번쩍인다.

**재측정**: 배포 후 6단계에서 행 0 왼쪽 아래, 행 1·2 오른쪽 아래. 좁혀졌다.

## 크레딧 복구와 새 에셋

계정이 swdau.ai02 로 바뀌며 크레딧이 **982** 로 돌아왔다. 클라이언트 편집본의
언어(짙은 남색, 청록·마젠타, 볼류메트릭, 얕은 심도)를 프롬프트로 고정해 네 장을
생성했다. 2크레딧 x4.

| 슬러그 | 무엇 | 왜 |
|---|---|---|
| `port_crane` | 부산항 컨테이너 터미널 야간 + 청록 경로선 | CHAIN 의 부산항 예시에 그림이 없었다 |
| `gpu_rack` | 서버랙 냉복도 + 청록 인디케이터 | 인프라(GPU 256장)에 로봇 손을 쓰고 있었다 |
| `lecture` | 계단식 강의실 뒷모습 + 대형 데이터 화면 | **대학 사이트인데 사람이 하나도 없었다** |
| `shipyard` | 조선소 도크 + 청록 계측선 | X축 확산(조선소로 옮긴다)에 그림이 없었다 |

검수: 읽히는 글자·로고 없음, 얼굴 없음(뒷모습만), 팔레트 일치. 이전에 라이브액션을
탈락시킨 항목(브랜드 마크, 인쇄된 글자, 이질적 색 의상)에 걸리는 것이 없다.

kling3_0_turbo 로 영상화 대기열에 걸었다 (10크레딧 x4). 배선은 다음 회차.

## 10회차 — 어두운 화면 넷이 연달아 나오고 끝나던 것

**측정**: 배포본 7단계. 우리 행 0~3 이 전부 어두운 전면 영상이다. 네 화면 연속
같은 톤. toss 는 사진·흰 바탕·사진·흰 바탕으로 계속 엇갈린다. 그리고 우리 홈은
네 구간에서 끝나는데 toss 는 스무 장을 간다.

toss 행 6 「토스가 바꾼 일상」은 사람 사진 넉 장이다. 앞이 전부 제품인데 끝은
사람이다. **우리 페이지에는 사람이 한 명도 없었다.** 대학 홍보 페이지에서 이건
그 자체로 결함이다.

**고침**: 두 절을 붙였다.

| 절 | 구성 | toss 근거 |
|---|---|---|
| 부산항 예시 | 밝은 바탕, 행마다 미디어·카피 좌우 교대 | 행 1 카피 왼쪽·제품 오른쪽 → 행 2 제품 왼쪽·카피 오른쪽 |
| 마무리 | 강의실 한 장면 전면 + 한 문장 | 행 6 「토스가 바꾼 일상」 |

부산항 시나리오는 네 학부가 어떻게 이어지는지 가장 잘 보여주는 내용인데
`/ai-college/adpx` 에만 있어 홈에서는 볼 수 없었다. 화면은 단계가 말하는 장소를
그대로 골랐다 — 하역 기록은 컨테이너 터미널, 예측 모델은 서버랙, 크레인 운영은
기어를 집어넣는 로봇, 확산은 조선소.

마무리를 toss 처럼 카드 넉 장으로 하지 않은 이유: 바로 앞 절이 이미 학부 카드
줄이라 카드를 또 놓으면 같은 화면이 두 번 된다. 한 장면을 통으로 썼다.

**배선**: 9회차에 생성한 네 컷을 kling3_0_turbo 로 영상화해 배선했다.
`port_crane_v` `gpu_rack_v` `lecture_v` `shipyard_v`. 포스터는 원본 스틸에서 뽑았다.
합계 48크레딧 (이미지 8 + 영상 40).

**재측정**: 배포 후 9단계.

| 행 | toss | 우리 |
|---|---|---|
| 4 | 밝은 제품 화면 | 흰 바탕 424 |
| 5 | 밝은 제품 화면 | 흰 바탕 표 |
| 6 | 어두운 제품 사진 | 카드 줄 |
| 7 | 어두운 제품 사진 | 좌우 교대 (미디어 왼쪽) |
| 8 | 「토스가 바꾼 일상」 사진 | 좌우 교대 (미디어 오른쪽) |

6단계에서 끝나던 페이지가 9단계까지 이어지고, 행 4~8 이 서로 다른 구도다. 좁혀졌다.

**남은 격차**: 행 0~3 은 여전히 어두운 화면 넷이 이어진다. 스크럽 히어로가
700svh 라 그렇고, 이건 클라이언트 편집본을 그대로 쓰기로 한 결과다. 줄이려면
히어로 길이를 손대야 하는데 그러면 컷마다 읽을 시간이 한 화면 아래로 떨어진다.

## 11회차 — 에셋 색감이 사이트와 안 맞던 것

**클라이언트 지적**: 에셋부터가 색감이 사이트랑 안 맞는다.

감으로 판단하지 않으려고 `scripts/hue.mjs` 를 만들었다. 채도·밝기가 일정 이상인
화소만 골라 색상각 히스토그램을 낸다. dominant 색은 배경(거의 검정)이라 팔레트
비교에 못 쓴다.

**재보니 지적이 맞았다.**

| | 색상각 | 브랜드와 차이 |
|---|---|---|
| 사이트 `#3182f6` | **215°** | — |
| 워프 (chain, schools) | 250° | 35° |
| 데이터 비 (axis_d, axis_stat) | 190° | 25° |
| 9회차 생성분 4장 | 190~200° | 15~25° |
| 자율주행차 (axis_x) | 200° | 15° |
| 입자 구름 (axis_a) | 220° | 5° |

9회차 생성분이 청록으로 간 이유는 **내 프롬프트가 "cyan and magenta rim light"
라고 못박았기 때문이다.** 브랜드색을 확인하지 않고 편집본 느낌만 보고 적었다.

**두 갈래로 고쳤다.**

*(1) 편집본에서 잘라 쓴 타일 — 색상 회전.* 히어로 원본은 건드리지 않는다.

  axis_d, axis_stat  +20  ·  axis_x  +12  ·  infra_gpu  +15

회전 폭은 눈으로 골랐다. axis_x 를 +15 로 돌렸더니 오른쪽 보라 패널이 분홍이 돼서
회전을 +12 로 줄이고 크롭을 x=120 으로 옮겨 그 패널을 화면 밖으로 뺐다.

워프는 -25 로 돌려도 줄기의 보라·녹색이 휘도에 박혀 안 빠졌다. -35 이상 돌리면
녹색이 더 섞인다. 채도를 눌러도 회색빛 보라가 남았다. **회전을 포기하고
브랜드색으로 새로 생성해 갈았다** (`warp_blue`).

*(2) 새로 생성 — 팔레트를 그림으로 물린다.* 프롬프트에 "azure blue" 라고 적는
것만으로는 또 청록으로 흐른다. `scripts/palette.mjs` 로 사이트 토큰 9단계 램프판을
만들어 `--image-references` 로 넣고, 청록·틸·마젠타·보라·녹색을 금지어로 박았다.

| 슬러그 | 무엇 | 왜 |
|---|---|---|
| `port_crane` `gpu_rack` `lecture` `shipyard` | 9회차분 재생성 | 190~200° → 210° |
| `campus_night` | 블루아워 공학관 | /ai-college 에 여는 화면이 없었다 |
| `studio_team` | 학생 넷 뒷모습 + 네트워크 도표 | 사람이 강의실 한 장뿐이었다 |
| `data_wall` | 차트·산점도 벽 | 통계학과가 데이터사이언스학부와 같은 화면을 써서 레일에서 두 카드가 거의 같아 보였다 |
| `roadmap_path` | 어두운 공간의 상승 경로 | 기존은 자율주행차 재탕 |
| `warp_blue` | 방사형 블루 버스트 | 250° 워프 대체 |

**두 번 다시 뽑은 것**: `studio_team` 첫 결과가 **학사모와 가운**을 입혔다.
2027년 출범이라 졸업생이 있을 수 없다. `roadmap_path` 는 흰 배경으로 나와 나머지와
언어가 달랐다. 둘 다 프롬프트에 명시적 금지를 넣어 다시 뽑았다.

**결과**: 전체 타일 색상각 폭이 **190~250° (60°) → 210~230° (20°)**, 브랜드 215° 중심.

**배선**: `MediaBand` 컴포넌트를 만들어 /ai-college 에 campus_night, studio_team 두
띠를 넣었다. 통계학과 카드는 data_wall 로 갈아 레일의 중복을 없앴다.

**크레딧**: 이미지 11장 22 + 영상 8편 80 ≈ 102. 잔액 ~640.

**겪은 것**: 백그라운드 큐 두 개가 같은 `assets/queue.json` 을 덮어써서 영상 6건이
사라졌다. 큐 파일을 나눠서 다시 돌렸다.

## 12회차 — 문장 하나만 놓인 화면이 없었다

**측정**: 배포본 8단계. toss 행 5 는 흰 화면 한가운데에 검은 형태 하나가 서 있고
그 좌우로 「온오프라인」「경계 없이」가 갈라져 있다. 다른 요소가 하나도 없다.
앞은 광고 카드 레일, 뒤는 제품 사진 — 둘 다 빽빽하고 이 화면이 그 사이에서
눈을 쉬게 한다.

우리에게는 그렇게 조용한 화면이 없었다. **모든 절이 눈썹 문구 + 제목 + 본문 +
보조 정보 네 겹을 한꺼번에 편다.**

**고침**: 표(규모·구조·준비 상황)와 학부 카드 줄 사이에 문장 하나만 놓인 화면.
toss 가 그 화면을 레일과 제품 사진 사이에 둔 것과 같은 자리다.

문장은 「AI는 한 학과가 감당할 수 있는 크기를 넘었어요」. 홈은 AI대학이 무엇인지,
몇 명인지, 어느 학부가 오는지는 말하는데 **왜 만드는지는 한 번도 말하지 않았다.**
그 답이 `/ai-college` 안쪽에만 있었다. 빈 자리 두 개를 한 번에 메운다.

toss 는 문장을 좌우로 갈라 사이에 오브젝트를 세우지만 우리는 한 덩이로 뒀다.
「온오프라인」「경계 없이」는 끊어도 각각 뜻이 서는 짧은 구인데, 우리 문장을
자르면 읽는 순서가 흔들린다.

처음에 88svh + 80px 로 잡았더니 앞 절의 아래 여백과 겹쳐 400px 넘는 빈 화면이
생겼다. 74svh + 64px 로 줄이고 표 절의 아래 여백을 걷었다.

**재측정**: 배포 후 10단계. **행 6 이 정확히 맞았다** — toss 「온오프라인 경계
없이」, 우리 「AI는 한 학과가 감당할 수 있는 크기를 넘었어요」. 같은 깊이에 같은
성격의 화면이 섰다. 좁혀졌다.

## 측정해 둔 것 — 절 높이 분포

다음 판단에 쓰려고 재 뒀다 (1280x800 기준).

| 절 | 높이 | 비중 |
|---|---|---|
| 히어로 | 5,600px | **40.9%** |
| Impact (부산항 + 마무리) | 2,180px | 15.9% |
| 학부 레일 | 1,920px | 14.0% |
| 표 | 1,100px | 8.0% |
| 424 | 714px | 5.2% |
| 숨 쉬는 화면 | 592px | 4.3% |
| 이미지 띠 | 413px | 3.0% |

총 13,689px ≈ 17화면. **히어로가 41%.** toss 는 히어로가 8~10% 다.

줄이려면 700svh 를 400svh 쯤으로 내려야 하는데, 그러면 컷당 스크롤이 50svh 로
한 화면 아래가 된다. 문구가 짧아 못 읽을 정도는 아니지만 완급이 급해진다.
클라이언트 편집본이 이 페이지의 강점이라 아직 손대지 않았다.

**힉스필드 대기열**: 미완료 없음. 11회차에 배선 끝. 잔액 ~635.

## 13회차 — 히어로가 페이지의 41%였다

**측정**: 11단계로 나란히 찍으니 우리 행 0~4 가 전부 히어로다. toss 는 그 자리에
사람 사진 / 폰 / 택배상자 / 결제 화면 / 카드 — 서로 다른 다섯 구도가 들어간다.

눈으로만 보면 다툴 여지가 있어 `scripts/sections.mjs` 를 만들어 픽셀로 쟀다.

| 절 | 높이 | 비중 |
|---|---|---|
| 히어로 | 5,600px | **40.9%** |
| Impact | 2,180px | 15.9% |
| 학부 레일 | 1,920px | 14.0% |
| 나머지 다섯 | — | 29% |

toss 히어로는 8~10% 다.

**고침**: 700svh → 500svh. **균등하게 줄이지 않았다.** 인트로는 스크롤하기 전부터
화면에 떠 있어 읽을 시간을 따로 줄 필요가 없고, 마무리는 CTA 두 개뿐이라 길게
붙잡을 이유가 없다. 그 둘만 가중치를 0.75 / 0.8 로 깎았다.

| 컷 | 전 | 후 |
|---|---|---|
| 연구실 (인트로) | 152svh | 89svh |
| 데이터 비 (D) | 118svh | 93svh |
| 입자 구름 (A) | 81svh | 64svh |
| 자율주행차 (X) | 80svh | 63svh |
| 산업 로봇 (P) | 103svh | 81svh |
| 엠블럼 (마무리) | 128svh | 80svh |

본문 컷이 63~93svh 를 받는다. 줄이기 전 가장 좁던 차(80svh)와 비슷해 읽을 시간은
유지된다.

**확인**: 22단계 캡처로 여섯 챕터가 전부 살아 있는 것을 봤다. 14단계에서는 차가
한 표본 간격보다 짧아 건너뛰어졌지만 화면에는 있다 — 표본이 못 잡은 것이지 사라진
것이 아니다.

**재측정**: 배포본 실측 **40.9% → 33.1%**. 총 높이 13,689 → 12,089px (17화면 → 15화면).
11단계 캡처에서 히어로가 5행 → 4행, 밝은 절이 7행을 차지한다. 좁혀졌다.

**남은 것**: 33% 도 toss 의 8~10% 와는 거리가 있다. 여기서 더 줄이면 컷당 50svh
아래로 떨어져 문구를 읽기 전에 다음 컷으로 넘어간다. 히어로를 더 줄이는 대신
뒤쪽 절을 늘리는 쪽이 남은 길이다.

**힉스필드 대기열**: 미완료 없음. 잔액 ~635.

## 14회차 — 출범을 내세우면서 거기까지 가는 길이 없었다

**측정**: 배포본 12단계. 히어로 뒤로 424 → 띠 → 표 → 숨 쉬는 화면 → 레일 →
부산항 → 마무리, 일곱 자리다. toss 는 같은 구간에 열둘을 놓는다.

13회차에 "히어로를 더 줄이는 대신 뒤쪽 절을 늘리는 쪽이 남은 길" 이라고 적어뒀다.
그 첫 번째로 무엇을 넣을지 고르는 기준은 **홈이 말하지 않고 있는 것**이었다.

이 페이지는 「2027년 3월 출범」을 첫 화면부터 마지막 화면까지 내세우는데,
**거기까지 어떻게 가는지는 홈에서 한 번도 말하지 않았다.** 추진 일정이
`/ai-college/roadmap` 안쪽에만 있었다.

**고침**: 부산항 절과 마무리 사이에 추진 일정을 넣었다.

*구도.* 서브페이지는 이미 점과 선으로 잇는 세로 타임라인이다. 홈에서 같은 그림을
또 그리면 두 화면이 겹친다. 여기서는 행마다 왼쪽 여백을 8% 씩 밀어 **계단**으로
세운다. 2027 행만 브랜드색 선과 글자로 도드라지게 뒀다.

5열 그리드도 재봤는데 1280 폭에서 한 칸이 236px 라 한국어 본문이 서너 글자마다
끊긴다. 계단은 각 행이 폭을 다 쓴다. 좁은 화면에서는 들여쓰기를 걷는다 — 세로로
쌓인 상태에서 들여쓰기가 남으면 계단이 아니라 어긋난 목록이 된다.

앞에 `roadmap_path` 띠를 뒀다. 11회차에 만들어두고 홈에서는 쓰지 않던 자산인데
다섯 표식이 점점 커지며 올라가는 그림이라 아래 계단과 형태가 맞물린다.

*구조.* 마무리(강의실)를 `ImpactScene` 에서 `ClosingScene` 으로 분리했다. 추진
일정이 마무리 앞에 들어가야 하는데 둘이 한 파일에 있어 순서를 바꿀 수 없었다.

*곁가지.* `ImpactScene` 의 alt 가 아직 「청록 경로선」이었다. 11회차에 색을 브랜드
블루로 옮겼으니 화면에 없는 색이다. 고쳤다.

**재측정**: 실측 히어로 **33.1% → 28.6%**. 총 높이 12,089 → 13,985px (15 → 17화면).
추진 일정 절이 10.2% 를 차지한다. 히어로 비중 추이 40.9% → 33.1% → 28.6%.

**힉스필드 대기열**: 12건 전부 완료, 미완료 없음. 잔액 ~635.

**남은 것**: 홈에 아직 없는 내용 — 가르치는 사람들(교원 60명, 43% 산업체·출연연
경력), 특화 프로그램(PNU AI특화 Pathway / AX1000 / AI Lab to Start-Up), 운영 체계
(PNU-APEX). 셋 다 `/ai-college` 안쪽에만 있다. 다음 회차 후보다.

## 15회차 — 누가 가르치는지 홈이 말하지 않았다

**측정**: 배포본 13단계. 히어로 뒤로 여덟 자리다. toss 는 열셋을 놓는다.

무엇을 넣을지는 14회차와 같은 기준 — **홈이 말하지 않고 있는 것**으로 골랐다.
홈은 몇 명이 들어오는지(424), 어느 학부가 오는지(다섯), 무엇을 배우는지(부산항),
언제 문을 여는지(추진 일정)까지 말한다. 그런데 **누가 가르치는지는 한 번도 말하지
않았다.** 대학을 소개하는 페이지에서 상위 세 질문 안에 드는 것이고, 답이 될 자료
(교원 60명)는 이미 갖고 있었다.

그리고 이 페이지에는 **사람 이름이 하나도 없었다.** 마지막 강의실 장면에 사람이
나오지만 실루엣이다.

**고침**: 학부 레일과 부산항 절 사이에 「가르치는 사람들」.

*구도.* 페이지가 이미 쓰는 구도가 아홉이다 — 전면 영상 / 큰 숫자 + 여백 / 전면
이미지 띠 / 라벨-값 표 / 문장 하나 / 가로 카드 레일 / 좌우 교대 / 계단 목록 /
전면 영상 + 문구. 여기에 또 카드를 놓으면 겹친다.

그래서 **이름을 그대로 벽처럼 폈다.** 60명을 학부별로 묶어 촘촘한 격자에 넣는다.
숫자 60 을 쓰는 것보다 이름 60 개가 놓인 화면이 더 강한 증거다. **지면 전체가
글자인 화면은 이 페이지에 아직 없었다.**

세 숫자(60 / 43% / 10)는 위에 두되 424 화면처럼 키우지 않았다. 이 절의 주인공은
숫자가 아니라 이름이다. 전공 줄은 한 줄로 자른다(`title` 에 전체를 남긴다) —
두 줄이 되는 사람이 섞이면 격자가 들쭉날쭉해진다.

**재측정**: 실측 히어로 **28.6% → 24.7%**. 총 높이 13,985 → 16,195px
(**17화면 → 20화면**). toss 와 같은 분량이다. 히어로 뒤 자리가 여덟 → 열.

히어로 비중 추이: 40.9% → 33.1% → 28.6% → **24.7%**

**힉스필드 대기열**: 미완료 없음. 잔액 ~635.

**남은 것**: 홈에 아직 없는 내용 — 특화 프로그램(PNU AI특화 Pathway / AX1000 /
AI Lab to Start-Up), 운영 체계(PNU-APEX), 협력 기업. 셋 다 `/ai-college` 안쪽이다.

## 16회차 — 길이는 맞췄는데 화면당 밀도가 달랐다

**측정**: 15회차에 20화면으로 toss 와 분량이 같아졌다. 그런데 14단계로 나란히
찍어보니 **toss 는 열넷 중 여덟 화면이 대상 하나만 크게 보여준다** — 폰 하나,
택배상자 하나, POS 단말 하나, 태블릿 하나. 곁들이는 문구는 한 덩이뿐이고 목록도
격자도 없다.

우리는 표에 여덟 줄, 레일에 카드 다섯, 이름 벽에 예순 명을 편다. **한 화면에
여러 개를 펴는 것이 기본값이었다.**

그리고 「들어와서 무엇을 할 수 있나요」가 `/ai-college` 안쪽에만 있었다. 진학을
생각하는 사람에게는 정원이나 교원보다 이쪽이 먼저 궁금하다.

**고침**: 두 가지를 한 절로 푼다. 특화 프로그램 셋을 **목록으로 늘어놓지 않고**
고정 무대에서 한 번에 하나씩 갈아 끼운다. 항목마다 화면 하나를 온전히 갖는다.

toss 는 절마다 화면을 새로 쌓지만 우리는 무대를 고정했다. 셋이 나란한 프로그램이라
자리를 옮기면 비교가 끊긴다.

**에셋** — 각 프로그램이 말하는 것을 그대로 그렸다.

| 슬러그 | 그림 | 문구 |
|---|---|---|
| `prog_pathway` | 갈라졌다 모이는 경로들, 그중 하나가 밝다 | 학습 경로를 직접 설계해요 |
| `prog_funnel` | 위는 넓게 흩어지고 아래로 갈수록 좁고 밝아지는 입자 기둥 | 넓게 시작해서 좁고 깊게 |
| `prog_startup` | 실험대 위로 떠오르는 장치와 빛의 고리 | 연구실 단위 기술창업 |

팔레트 참조판을 물려 전부 200~210° 로 나왔다. 2크레딧 x3 + 10크레딧 x3 = 36.

**재측정**: 실측 히어로 **24.7% → 20.6%**. 총 높이 16,195 → 19,395px
(20화면 → 24화면). 히어로 뒤 자리가 열 → 열셋.

히어로 비중 추이: 40.9% → 33.1% → 28.6% → 24.7% → **20.6%**

**힉스필드 대기열**: 미완료 없음. 잔액 ~599.

**남은 것**: 홈에 아직 없는 내용 — 운영 체계(PNU-APEX), 협력 기업(NAVER Cloud /
Google / AWS / LG U+ / upstage). 둘 다 `/ai-college` 안쪽이다.

## 17회차 — 아홉 화면 연속으로 전면 이미지가 없었다

**측정**: 배포본 16단계. 전면 이미지가 행 0~4 와 행 14 이후에만 있다.
**행 5부터 13까지 아홉 화면 연속으로 전면 이미지가 하나도 없다.** 표 → 숨 쉬는
화면 → 레일 → 이름 벽 → 부산항 → 프로그램이 전부 흰 바탕 안쪽에서 끝난다.
toss 는 흰 화면과 전면 사진을 계속 번갈아 간다.

**먼저 확인한 것**: 캡처의 흰 자리가 레이아웃 구멍인지부터 쟀다. 열한 개 절 경계가
**전부 0px** 다. 흰 부분은 절 안쪽 여백과 표본 경계지 결함이 아니다. 되돌릴 것이 없다.

**고침**: 이름 벽과 부산항 절 사이에 협력 기업 전면 화면. 그 자리에 마침 홈에 없던
내용이 하나 있었다. 이름 벽(누가 가르치나) 다음에 「수업에 기업이 들어와요」가 오면
사람 이야기가 학교 밖으로 이어진다.

**에셋** `industry_night` — 밤의 임해 산업도시를 높은 곳에서 내려다본 화면. 왼쪽에
항만 크레인, 가운데 공장, 오른쪽 사무동이 서고 건물 사이를 파란 선이 잇는다.
동남권이라는 배경과 "기업과 이어진다"는 내용이 한 화면에 들어간다. 210° 82%.
2 + 10 = 12크레딧.

**로고를 안 쓴 이유**: 기업 이름은 테두리 있는 글자로만 적는다. 생성 이미지에 상표를
그려 넣는 것은 물론이고 실제 로고 파일을 쓰는 것도 사용 허락이 필요한 일이다.
대학이 공개한 자료에 적힌 이름을 그대로 옮기는 선에서 멈춘다.

**재측정**: 전면 이미지가 행 0~3 / 4 / **9** / 14 / 끝으로 흩어졌다. 아홉 화면 연속이
끊겼다. 실측 히어로 **20.6% → 20.1%**, 총 높이 19,395 → 19,939px (24 → 25화면).

히어로 비중 추이: 40.9% → 33.1% → 28.6% → 24.7% → 20.6% → **20.1%**

**힉스필드 대기열**: 미완료 없음. 잔액 ~587.

**남은 것**: 홈에 아직 없는 내용은 운영 체계(PNU-APEX) 하나뿐이다. 다만 25화면이면
toss(20화면)를 이미 넘었다. 다음 회차부터는 길이를 더 늘리는 쪽보다 화면 하나하나의
완성도를 보는 쪽이 맞다.

## 18회차 — 18회차 동안 모바일을 한 번도 안 봤다

17회차에 "길이를 더 늘리는 쪽보다 화면 하나하나의 완성도" 로 방향을 바꾸겠다고
적었다. 그 첫 번째로 **한 번도 확인하지 않은 것**부터 봤다.

**모든 캡처가 1280x800 이었다.** 홍보 페이지를 실제로 여는 화면은 대부분 손안인데
18회차 동안 한 번도 보지 않았다. `scripts/mobile.mjs` 를 만들어 390x844 로 훑었다.

**먼저 확인한 것**: 문서 가로 넘침 없음 (390/390). 프로그램 절 내용 550px / 화면
844px — 넘침 없음. 대부분은 멀쩡했다.

**찾은 결함**: 학부 레일.

- 카드가 264px 인데 화면이 390px 라 **어느 스크롤 위치에서도 카드가 좌우로 잘려
  있다.** 온전한 카드를 한 장도 못 본다.
- 컨테이너가 `overflow-hidden` 이라 **손가락으로 밀어도 아무 반응이 없다.** 가로로
  늘어선 것을 밀어 보는 것은 손안에서 가장 먼저 나오는 동작인데 죽어 있었다.

**고침**: 좁은 화면에서는 스크롤 구동을 끄고 평범한 가로 스크롤 + 스냅으로 돌렸다.
절 높이도 240svh 고정을 걷고 `auto` 로 뒀다 — 스크롤로 밀지 않으니 여분의 스크롤
거리가 필요 없다.

데스크톱은 그대로다. 마우스에는 가로로 밀 방법이 마땅치 않아 세로 스크롤이 미는
편이 낫고, 잘린 카드가 "더 있다"는 신호도 거기서는 유효하다.

**중간에 잡은 것**: `snap-start` 만 걸었더니 첫 카드가 화면 끝에 붙었다. 스냅은
항목을 스크롤포트 시작선에 맞추므로 `ul` 의 좌측 여백만큼 스크롤이 밀린다.
`scroll-pl-6` 으로 그 여백을 스냅 기준선에 알려줬다.

**재측정**: 390px 에서 첫 카드가 여백을 두고 온전히 서고 다음 카드가 살짝 보인다 —
밀라는 신호다. 데스크톱 캡처에서 기존 스크롤 구동 동작은 그대로다.

**다음 회차 후보**: `scripts/mobile.mjs` 로 서브페이지(`/ai-college`,
`/ai-college/adpx`, `/ai-college/schools/:slug`, `/ai-college/roadmap`)도 훑어야 한다.
홈만 봤다.

## 19회차 — 서브페이지 모바일

18회차에 홈만 390px 로 봤다. 서브페이지 네 곳(`/ai-college`, `/ai-college/adpx`,
`/ai-college/schools/:slug`, `/ai-college/roadmap`)을 마저 훑었다.

가로 넘침은 네 곳 다 없었다(390/390). ADP+X 축 탭, 학부 상세의 교원 카드, 계단
일정 전부 좁은 화면에서 정상이다. 대신 둘이 나왔다.

**(1) 전면 이미지 띠가 조각이 된다**

21:7 로 잡아뒀는데 좁은 화면에서는 370px 폭에 높이가 **162px** 밖에 안 된다.
건물도 길도 무엇인지 알아볼 수 없다. 데스크톱 1240x413 과 나란히 두면 차이가 분명하다.

`MediaBand` 기본값과 `FactSheet`·`Roadmap` 의 띠에 모바일 단계를 따로 뒀다.
`aspect-[3/2] sm:aspect-[16/7] md:aspect-[21/7]`.
실측 **370x162 → 370x247**. 데스크톱은 1240x413 그대로다.

**(2) 「추진 일정」 옆에 자율주행차가 달리고 있었다**

`/ai-college/roadmap` 의 그림이 편집본에서 잘라낸 자율주행차였다. 11회차에 이
자리에 맞는 그림(어두운 공간을 지나는 파란 길, 점점 커지는 다섯 표식)을
`roadmap_path_v` 로 새로 만들면서 **포스터만 갈아 끼우고 영상 슬러그를 두고 갔다.**
홈은 새 그림, 서브페이지는 옛 차 영상이 나오고 있었다. 회차 사이에 자산을 갈 때
같은 슬러그를 쓰는 곳을 전부 훑지 않으면 이런 것이 남는다.

슬러그를 바꾸고 액자 배경도 회색에서 검게 맞췄다 — 밝은 상자에 짙은 화면을 넣으면
레터박스가 생긴다. 밀려난 차 영상은 `assets/unused` 로 옮겼다.

**힉스필드 대기열**: 미완료 없음. 잔액 ~587.

**남은 것**: 홈에 없는 내용은 운영 체계(PNU-APEX) 하나. 모바일은 다섯 페이지 다 봤다.
다음은 태블릿 폭(768~1024)이 남았다 — 한 번도 보지 않았다.

## 20회차 — 태블릿 폭

19회차까지 390 과 1280 만 봤다. **768~1024 는 한 번도 보지 않았다.**
`scripts/mobile.mjs` 에 폭 인자를 붙여 768 / 820 / 1024 를 훑었다.

가로 넘침은 세 폭 다 없었다. 대신 둘이 나왔다.

**(1) 태블릿에서 레일을 밀 수 없다**

18회차에 좁은 화면용 가로 스크롤을 붙였는데 기준이 `max-width: 767px` 였다.
태블릿은 768 을 넘어 **데스크톱으로 잡히면서 `overflow-hidden` 인 채로 남았다.**
손가락으로 밀어도 반응이 없다.

밀 수 있느냐는 화면 크기가 아니라 **포인터 종류**의 문제다. `useIsTouch` 를 더해
`(pointer: coarse)` 면 폭과 무관하게 네이티브 가로 스크롤로 간다.

**(2) 프로그램 무대가 태블릿에서 가장 작아진다**

좌우 두 칸을 `md`(768) 부터 나눠뒀더니 820x1180 에서 무대가 **화면 높이의 17%**
가 됐다. 네 폭 중 가장 작다. 한 화면에 하나만 크게 보여주려고 만든 자리인데
정반대가 됐다.

두 칸을 `lg`(1024) 부터로 미뤘다. 820 에서 세로로 쌓이며 무대가 **40%** 를 받는다.

| 폭 | 전 | 후 |
|---|---|---|
| 390 | 26% | 26% |
| 820 | **17%** | **40%** |
| 1024 | 22% | 22% |
| 1280 | 24% | 24% |

내용 높이도 같이 쟀다. 네 폭 전부 화면 안에 들어간다(820 에서 842px / 1180px).

**재측정**: 배포본 390 / 820 / 1280 에서 문서 폭 = 화면 폭. 데스크톱 프로그램 절은
카피 왼쪽·미디어 오른쪽 그대로다.

**힉스필드 대기열**: 미완료 없음. 잔액 ~587.

**남은 것**: 홈에 없는 내용은 운영 체계(PNU-APEX) 하나. 폭은 390 / 768 / 820 /
1024 / 1280 을 봤다. 아직 안 본 것 — 실제 기기의 저속 회선, 그리고 `prefers-reduced-motion`
을 켠 상태. 후자는 코드에 분기가 있지만 화면으로 확인한 적이 없다.
